## Notes

### Basic Props
- name: string
- messageCount: number
- isLoggedIn: boolean
- person: { first: string, last: string }
- personList: {
    first: string,
    last: string
  }[]

For array, we can use: <br>
a: string[], a: number[], a: boolean[], etc.